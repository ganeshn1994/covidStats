import { TableHeadPipe } from './table-head.pipe';

describe('TableHeadPipe', () => {
  it('create an instance', () => {
    const pipe = new TableHeadPipe();
    expect(pipe).toBeTruthy();
  });
});
